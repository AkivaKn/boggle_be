{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Boggle API",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/api/main.go",
            "env": {
                "DATABASE_URL": "postgres://devuser:devpassword@db:5432/boggle?sslmode=disable"
            },
            "showLog": true
        }
    ]
}